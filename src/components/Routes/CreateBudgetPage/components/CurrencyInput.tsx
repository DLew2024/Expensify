import { PrimaryInput } from '../../../Input';

export const CurrencyInput = () => {
	return (
		<div>
			<PrimaryInput type="text" placeholder="Starting balance..." title="" />
			{'Currency input'}
		</div>
	);
};
